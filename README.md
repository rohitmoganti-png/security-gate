# security-gate

AI-native commit security gate: a reusable GitHub Actions workflow that scans every push/PR for secrets, risky code, fake packages, vulnerable dependencies and infra misconfigurations.
